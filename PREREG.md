# PREREG · 预注册（v0）

状态：**v0 草案**（2026-07-17）。划分冻结（`scripts/wp2_freeze_splits.py`）并回填指纹
（`scripts/prereg_fingerprint.py`）后打 tag `prereg-v0`；E1 全量启动前升级冻结为 `prereg-v1`。
判据全文以 `文档/00_原始计划.md` §1.2 为权威，本文登记数值判据、划分、种子与变更流程；
探索性分析一律在报告中单列，不得事后并入确证性判据。

## 1. 假设与数值判据（摘自 00 §1.2，冻结）

- **H1 可解码性与前瞻性**：最优层线性探针 AUC − max(声学基线, 编解码/编码器基线, hazard 基线) ≥ **0.05**，
  会话级 cluster-bootstrap 95% CI 下界 > 0；在模型自身决策时钟 ≥ 3 步前瞻仍成立。
  （操作化注记，#8 δ 读法裁决 2026-07-18：**前瞻按观测截止到事件的净时间计**——G1 的 T1
  判据点 δ=400 → 净前瞻 [320,400) ms ≥ 3 步；δ=240（净 [160,240) ms）为描述性附表。）
- **H2 因果性**：patching/方向注入翻转率 ≥ **50%** 且高出范数匹配随机方向 ≥ **30 pt**；剂量单调
  （Spearman ρ > 0.8）；内容方向对照无效（双重分离）。
- **H2b 整合器动力学**：漏积分拟合时间常数预测让位延迟分布（KS 检验不拒绝）。
- **H2c 通路中介**：top-k 用户流监测头中介效应比 ± CI 可量化；发言门控直接 logit 归因通路给出。
- **H3 可控性**：存在 α 使停顿处理 TOR ↓ ≥ **30%**、打断响应 TOR 不降、GPT 内容分 ↓ ≤ **5%**、
  UTMOS ↓ ≤ 0.15；SDT d′ 保持率不劣于 PAD/静音 logit 偏置。
- **H4 信息基础**：语义最小对差分显著（置换 p < 0.01），能量匹配对照不显著；2×2 交互项显著。
  全线不成立 → N3 反例论文路线。
- **H5 收敛性**：(a) 同架构方向可移植；(b) 跨架构 CKA/Procrustes 显著高于随机匹配；
  (c) 双语模型内跨语言探针迁移 AUC 降幅 ≤ 0.05。

## 2. Gate 判据（摘自 00 §7，冻结）

| Gate | 判据 | 过 → | 不过 → |
| --- | --- | --- | --- |
| G0 | 四条件（变更记录 #9 冻结，2026-07-18）：层1 官方算法逐帧全等 = 1.000（固定 commit 2d0db21）∧ 层2 确认集双通道 VAD F1 ≥ 0.90 ∧ 层3 语料级 macro-F1 ∈ **[0.48, 0.56]**（等价带；高出带 = 红旗调查，不自动通过）∧ 会话级 macro-F1 P10 ≥ 0.30（linear 分位）。确认集 = test 除目录序前 20 探索集外的 118 会话，一次性运行（`wp1_g0_calibrate.py --confirmation`） | MVE | 修实现（≤1 周，参数不动） |
| G1 | 最优层 AUC 优势 ≥ +0.05 且 CI 下界 > 0（T1/T4 取较大者；T1 判据点 δ=400，净前瞻 [320,400) ms）；执行协议见变更记录 #7/#8（text_mode=greedy、LMGen 在线对齐锚定、嵌套选择、δ 净毫秒读法） | E1 全量 | +0.02~0.05 备胎 MVE；全部 <+0.02 → N1 |
| G2 | 跨 3 种子 top-3 层重叠 ≥ 2；有效秩 ≤ 16；方向余弦 ≥ 0.8 | E2 | 换位点重试 → N1' |
| G3 | H2 四判据 | E3 + G3b | N2 |
| G3b | top-8 头中介 ≥ 70% 且 Jaccard ≥ 0.6 | Tier-3 入正文 | Tier-3 后续工作 |
| G4 | H4 语义敏感 | 摘要 A | N3（摘要 B） |
| G5 | 完成度与日期 | ICLR 2027 | ICML 2027 |

## 3. 划分与种子（00 §4.3，生成即冻结）

- CANDOR：60/15/25（probe_train / probe_val / causal_eval），会话级；**causal_eval 侧在 E2 之前绝不读取**。
  已冻结（2026-07-17）：1,656 会话 → 994/248/414。
- SmoothConv：70/30（train / eval_sdt）；已冻结（2026-07-17）：2,503 会话 → 1,752/751。
  DuplexConv 仅训练侧（标注形态待定位，暂缓冻结）；DualTurn-SWB 沿官方 `splits.json`。
- **DualTurn 集合纪律（2026-07-17，变更记录 #1）**：G0 层3 门槛用官方 **val（295 会话）**估计；
  test 的前 20 个会话（按目录序）已在首轮联调中消耗，登记为**探索集**；
  其余 **118 个 test 会话锁定为一次性确认集**，门槛冻结前任何分析不得读取。
- 划分种子：**20260717**；刺激主种子：**20260717**（configs/stimuli.yaml master_seed）。
- MVE 抽样：probe_train 前 160 段 + probe_val 前 40 段（按冻结划分文件内的排序），每段前 10 min。
- R2 生成制式（v0 默认，E1 冻结前可经变更记录修订）：温度 **0.8**、种子 **0**；
  温度敏感性附 {0.6, 1.0}（00 §8）。
- 探针协议：线性 L2-logistic，C ∈ {0.01, 0.1, 1}（验证段选择），3 种子 {0,1,2}，负类下采样 5:1，
  会话级 cluster bootstrap 1,000 次；帧级重采样禁止。

## 4. 冻结参数指纹

由 `uv run python scripts/prereg_fingerprint.py` 自动回填（configs/*.yaml 与 configs/splits/*.json 的 sha256）。

<!-- FINGERPRINT:BEGIN -->

指纹生成时间：2026-07-19T09:53:06.483598+00:00

| 文件 | sha256 |
| --- | --- |
| configs\events.yaml | `1eaeb1b762494f9bfa61f9b89b2d80bba5cc273cb01f56110af06630b6008675` |
| configs\grids.yaml | `d7f64569148c1123856aabe2eafc906d1c84844ddda968942643f3ce17257813` |
| configs\paths.windows.yaml | `f4523178302f90b513740e220238890b605261ee4ca38b22ab5949a5aa581b0d` |
| configs\splits\candor.json | `0378ca258a5daebbf9bc5c966e552da7640a90499f2b3d9f0b21b465be93c02e` |
| configs\splits\smoothconv.json | `5e8612883f3eb671a6a58d51707cd0d5a3f23232725338e9d7832e13730991b7` |
| configs\stimuli.yaml | `50b86781124caa6b0fac27fd78f8fa5d361596b27a6c2108829c2828ea6703f5` |
<!-- FINGERPRINT:END -->

## 5. 结果登记

| Gate | 日期 | 结果 | 证据 |
| --- | --- | --- | --- |
| G0·层1 | 2026-07-18 | 参考实现 138/138 会话逐帧全等（1,030,734 通道帧残差 0；与官方源码 2,000 组随机对拍一致）；库版在 test 探索集 20 会话及发布物可用的官方 val 295 会话上复核，四类逐帧残差均为 0 | reports/g0_reference_recompute.json；reports/g0_protocol_check.json；reports/g0_summary.json |
| G0·层3 | 2026-07-18 | 发布物可用的官方 val 295 会话诊断 macro-F1 = 0.520921；层2双通道 VAD F1 = 0.917724 / 0.918477；门槛仍待按语料级 Gate 同口径冻结，未运行剩余 118 会话的层2/3确认 | reports/g0_summary.json；reports/g0_校准报告.md |
| G0（正式确认） | 2026-07-19 | **fail**：118/118 会话完整运行；层1 macro-F1=1.0000 通过，层2 VAD ch0/ch1 F1=0.899457/0.904984（ch0 低于 0.90）失败，层3语料级 macro-F1=0.476885（低于 [0.48,0.56]）失败，会话级 P10=0.307888 通过。无上界红旗，最终为硬失败 | reports/g0_confirmation.json；reports/g0_确认报告.md |
| G1（首轮） | 2026-07-18 | ~~**N1**~~ **已撤回（2026-07-18，用户裁决，变更记录 #7）**：该轮 400 路缓存全部 text_mode=pad（违反冻结的贪心自预测文本流协议），且主探针与 Mimi/hazard/声学基线存在约一帧观测截止错位（基线多看 ≈80 ms、细码本最坏 ≈160 ms 未来音频），偏差方向系统性利于 N1。撤回的数字（T1 −0.011750 [−0.015747, −0.007859]；T4 −0.032093 [−0.042637, −0.021938]）只描述 PAD+错位变体，其算术复算本身无误 | 变更记录 #7；历史提交 `9d1f03a` |
| G1（全窗 greedy 重跑） | 2026-07-19 | ~~**N1**~~ **已被 #11 取代（2026-07-19，用户裁决：超上下文协议缺陷）**：运行 7500 步超出 Moshiko 官方 context=3000 步——行 3000 出现 80/80 一致的巨范数替代 sink 尖峰（与初始状态方向余弦 0.9985），行 6000 后深层进入跨会话公共吸引态（公共方向占比 0.50→0.85）；冻结分数包分段复算 T4 优势 0–6000 步 +0.02401 [+0.01415,+0.03516] vs 6000+ 步 −0.06006 [−0.08027,−0.03765]，CI 完全分离，Mimi 全程稳定排除标签/音频侧解释。历史数字（T1 +0.000766 [−0.002210,+0.003356]；T4 +0.006609 [−0.001638,+0.015983]；34/34 独立复算零差异）只描述全窗变体，其算术无误。状态：**截断重裁决待重跑（#11）** | 变更记录 #11；历史提交 `9386933`；reports/mve_上下文分段诊断.*（工具固化后回填） |
| G1·PAD 消融（全窗） | 2026-07-19 | **仅作非裁决性消融（全窗变体，随 #11 一并被取代）**：正确时间对齐与 δ=400 下，T1 优势 −0.009751 [−0.012941,−0.006660]，T4 优势 −0.032492 [−0.044636,−0.021131]；PAD 明显压低探针相对 Mimi 基线的表现并改变最优层 | 历史提交 `9386933` |
| **G1（#11 截断窗正式重裁决）** | 2026-07-19 | **full_e1**：决定性目标 T4——最优层 L20，探针 AUC 0.817719 vs 最强基线 Mimi 0.760546，优势 **+0.057173**，95% CI [+0.044127, +0.069435]（≥ +0.05 且 CI 下界 > 0）→ **全量 E1（五模型展开）**。T1 最优层 L28，优势 +0.014017 [+0.010026, +0.017840]（单目标 n1）。独立审计 passed（34/34 分数项、400 manifest、截断行域、冻结配置、bootstrap 复算全通过）；用户并行化实现与串行实现的 overall/per_target/descriptive 三区块逐字符一致。描述性佐证：δ240 +0.012509 [+0.009431,+0.015490]；matched-Mimi T4 +0.061968 [+0.048187,+0.075381]（同帧输入特权占比小，优势非基线信息劣势伪影）；分段诊断固化（T4 规格内 +0.02949、二次淘汰后 −0.05999）。PAD 消融同窗复跑 T4 +0.055661 [+0.040705,+0.069118]——**结论对文本流选择稳健**。选中层 C 全部落在网格内部（T4 L20 = 0.001；浅层非选中格触新下界 1e-4，已登记不影响裁决） | reports/mve_summary.json；reports/mve_报告.md；reports/mve_independent_audit.json；reports/mve_上下文分段诊断.json/.md；reports/mve_summary_ablation_pad.json |
| T4 标签人工核验（#13） | 2026-07-19 | **通过（用户人工认证）**：用户完成盲听评判，认证生产链启发式 TURNEND 标签足够可靠；#13 两条处置线（matched∪prod_only 确认率 <0.80 / backbiter_only 确认率 ≥0.50）均未触发。**PREREG #1/#7 的 T4 风险注记解除**。登记注：正式 `ingest` 汇总报告未生成，本行以用户 2026-07-19 认证为准；Backbiter 分歧归因于弱参考自身噪声 | 用户 2026-07-19 人工认证（会话回传）；抽样材料 D:\data_storage\...\t4_human_audit\ |

## 6. 变更记录

任何冻结参数/判据的变更必须先在此登记（日期、条目、理由、影响面、需重跑的校准），再改配置。

| 日期 | 状态 | 条目 | 理由 | 影响面 | 重跑 |
| --- | --- | --- | --- | --- | --- |
| 2026-07-17 | **已批准（修改版，用户裁决）** | **#1 G0 判据分层化（修改版）**：层1 协议正确性 = 固定官方提交 `2d0db21e767b953f5017c1cc697928b54161d645` 的 `relabel_context_aware.py`，要求逐帧全等（参考移植 `scripts/wp1_g0_reference_recompute.py` 已 138/138 全等；库版 `events/g0_official.py` 为第二实现，`--protocol-check` 持续对拍）；层2 VAD 一致性审计保留；层3 端到端门槛用官方 **val 295 会话**估计后冻结。集合纪律：test 前 20 会话（首轮联调已读）登记为探索集，其余 118 个锁定为一次性确认集。另：生产链 `process_session → detect_all`（事件本体，供 CANDOR/SmoothConv）仍需独立原始音频验证，不因 G0 重构而免检 | 用户 G0 首轮复核 + 层1 网格取证（64 组无解、结构性错误三处：无人恢复漏标 HOLD、BOT 最近活动语义、BC 首尾静音）；参考移植 138/138 全等确立语义权威 | G0 判据（00 §7）；`configs/events.yaml` g0 节；`events/g0_official.py` | 库版 `--protocol-check`（探索集 20 → val 295）→ val 三层全量定门槛 → 118 确认集一次性运行 |
| 2026-07-17 | **已批准（修改版，用户裁决）** | **#2 S1 拆臂（修改版）**：同文本原版/F0 拉平变换对的时长判据 ±5% 批准；complete/incomplete 字面前缀臂**不能单独作为 H4/G4 确证证据**。S1 拆为：**S1-A 确证臂**——跨条目配对 complete_i vs incomplete_j，总时长与语音活动时长均 ±5% 匹配（`stimuli/pairing.py` + `wp6_build_stimuli.py pair-a`）；**S1-B 探索臂**——保留真实前缀对，完整保存方向性时长比、韵律指标与人工抽听结果，仅作探索性分析 | V6 首轮质检 30% 通过率根因（前缀对时长判据结构性不可能）+ 用户裁决：前缀臂的时长/信息量混淆使其不足以单独确证语义敏感性 | S1 设计（00 §5）；`configs/stimuli.yaml`；E4/G4 证据结构 | 全量合成后跑 `pair-a` 报告配对成功率；S1-A 对数不足时扩充文本模板库 |
| 2026-07-18 | **已批准（用户裁决）** | **#3 MVE 长序列执行改为有状态分块**：跳过曾伴随两次 Windows 重启的 600 秒整段压力测试；Mimi 按官方在线路径固定为每次 1 帧（0.08 秒），主 transformer 固定 128 时钟步块，均在单次会话内保持官方 streaming 状态；delay 在完整时间轴全局施加一次；量化前连续潜表征显式使用 `quantize=False`；生产缓存单卡串行、按完整 run 的原子 manifest 断点续跑。该变更只约束执行方式，不改前 10 分钟样本域、层位、标签、探针、基线或 G1 判据 | 两次整机重启均发生于 600 秒整段 `lm(codes)` 压力路径；用户明确要求跳过该压力测试，并将其登记为 Windows 未知底层故障。原实现的 4096 步“分片”发生在整段前向完成后，不能约束峰值资源。20 秒块长扫描显示官方单帧 streaming 与离线量化前潜表征余弦最高（0.999980），运行实时系数 0.065；更大流式块产生更明显边界偏差 | `configs/grids.yaml` MVE 工程块长；Moshi R1 runner；MVE 缓存计划与 manifest | 同一段 20 秒音频取证：Mimi 量化前潜表征相对离线参考余弦 ≥0.9999，离散码差异率完整报告且 ≤5%（生产语义以官方逐帧 streaming 为权威）；分块主干四层相对官方整段 `forward_text` 余弦均 ≥0.9999。通过后重跑全部 400 个 R1 缓存与 ingest；不运行 600 秒整段压力路径 |
| 2026-07-18 | **已批准（用户指令 + 冻结实现纠错）** | **#4 MVE 双卡互斥分片与 G1 有界裁决**：400 路缓存静态切为卡1 150路、卡2 250路，分片输出交集为0且每张卡同一时刻只运行一个完整 run；G1 按目标、层位串行，训练标签先按旧全局顺序与同一随机种子确定性负采样，再只读取入选特征行，验证与评估逐会话/小批执行。hazard 固定为无未来泄漏的8列协变量：距最近任一 ONSET、任一 OFFSET、独占说话人切换、当前可观测态持续时间及 {SPEAK, LISTEN, OVERLAP, GAP} one-hot；YIELD/HOLD/UNRESOLVED 统一为当下 OVERLAP。正式预检须核对400个 run 的代码指纹、600/0.08/128 流式字段、状态保持、全局 delay、量化前 latent、当前源 WAV 哈希及200份标签完成标记。T4 在前600秒无对方 IPU 末点时允许角色或会话形成合法空集：保留为零行会话级 cluster，不补样本、不换划分；T1/T4 改在冻结 train/val 池层面各自强制正负类齐全，T1/T5 的逐角色完整性护栏保持。三基线均报告 AUC/AUPRC/balanced accuracy，G1 优势仍只使用最大基线 AUC | 用户明确要求用两张显卡按150/250均衡长缓存；旧 G1 会一次物化约127 GiB四层特征，且声学 GRU 验证整批前向会分配约7.2 GiB隐状态；旧 hazard 含重复时间列与绝对录音时刻，偏离冻结定义。冻结标签中有9个合法空 T4 角色，旧预检会将其误报为数据损坏并使正式运行必然失败。上述问题均可在正式裁决前纠正，且不改变会话、前10分钟样本域、层位、目标、种子、负采样比例、三基线集合、bootstrap或Gate阈值 | MVE 缓存调度；`wp7_run_mve.py`；数据集装配；hazard/GRU基线；G1输入预检与报告 | 20秒流式等价护栏已通过；缓存全量重跑后执行 zarr round-trip ingest；单元回归、抽样等价与内存上界测试全绿后，仅运行一次正式 G1 |
| 2026-07-18 | **已登记（严格预检触发的实现纠错）** | **#5 帧级标签唯一键与事件缓存代码指纹**：T1–T5 标签出口以 `(target, agent_channel, step, delta_ms)` 为唯一键；多个合法 episode 覆盖同一时钟步时，字段完全一致的行收敛为一个监督观测，同键任一字段冲突则硬失败。事件缓存输入指纹同时绑定配置、源音频状态与事件管线源码内容；源码变化必须令旧完成标记失效并自然重算 | 首次正式 G1 在任何模型拟合前被严格预检拦截：169 个会话含 T2 重复键。全量取证确认共 2,688 个键、每键恰好两条完全相同行、冲突数为0，T1/T4/T5 均无重复；根因是两个合法重叠 episode 的 T2 监督窗口覆盖同一帧。旧缓存指纹只绑定配置与音频状态，无法感知标签代码修复 | `events/labels.py`；事件管线双角色出口；`wp1_run_events.py` 完成标记；G1 输入预检 | 最小重叠 episode 复现由24行收敛为17个唯一键；冲突键单测硬失败；代码内容变化令旧标记不可复用。重算全部事件标签后要求所有会话唯一键零重复、完成标记哈希全等，再重启同一正式 G1；不改冻结划分、阈值、T1/T4样本、负采样、基线、bootstrap或Gate |
| 2026-07-18 | **已登记（正式 G1 前的统计与基线口径纠错）** | **#6 Mimi 双通道角色基线与多种子会话级统计**：Mimi 基线对每个角色固定拼接 `[self, other]` 两路连续潜表征，agent0 使用 run0+run1、agent1 使用 run1+run0；复用现有400路缓存。Mimi 线性探针与主探针同用种子 `{0,1,2}` 及各自的确定性训练抽样计划。层选择仍按各层种子 AUC 均值；正式点估计、probe CI 与成对优势 bootstrap 在每次会话重采样内分别计算各探针种子的 AUC 后取种子均值，hazard/声学 GRU 作为单种子基线，优势定义为主探针种子均值减去各基线种子均值中的最大值。报告明确种子数、均值±SD，并将 CI 说明为给定已在 probe_val 上选择目标/层/C 后的会话级条件 CI，不覆盖模型选择不确定性 | 旧 Mimi 基线仅读取目标角色自己的 `mimi_latent`，遗漏双通道信息且角色信息域与主模型输入不一致；旧 Mimi 仅运行种子0；旧 bootstrap 以中位种子作为代表，导致点估计与用于选层的 `auc_mean` 口径不一致。两角色各自缓存已分别保存对应通道潜表征，可在读取时按角色拼接，无需重跑缓存 | `mve/dataset.py` 双通道读取与对齐护栏；`mve/run.py` 多种子统计；`wp7_run_mve.py` Mimi 多种子编排与报告；相关单元测试 | 不重跑400路原始缓存或 ingest。正式 G1 前要求双通道顺序、双数组时间/步号/维度、Mimi三种子独立抽样计划、每种子严格对齐、种子均值点估计与会话级 bootstrap 单测全绿；随后仅运行一次正式 G1 |
| 2026-07-18 | **已批准（用户裁决：撤回 N1 并纠错重跑）** | **#7 G1 撤回与协议纠错重跑**：(a) **撤回首轮 G1/N1 正式裁决**——400/400 缓存 text_mode=pad 违反冻结的"贪心自预测、不采样"文本流协议（文档/02 WP4），且主探针（acts[s] 观测 ≤ s·τ）与三基线（Mimi latent[s]/hazard[s]/声学窗尾 s 均观测 ≤ (s+1)·τ）存在一帧观测截止错位，两项偏差都只削弱主探针。(b) **时间对齐统一**：标签步 s 观测截止冻结为 s·τ；acts 读行 s，Mimi/hazard/声学读行 s−1；step 0 全表征剔除（`mve/alignment.py` 单一权威 + 哨兵因果测试；runner manifest 声明 `time_alignment`，预检核验）。δ 冻结定义不变——y[s]=步 s+3 内 onset 相对观测截止恰为 [240,320) ms。(c) **文本流实现**：runner 实现 greedy（逐步自预测，text_linear 贪心 argmax 反馈为下一步文本输入）与 sampled（AB 用，固定温度/种子）；`grids.yaml` 新增 `text_mode: greedy` 冻结字段；缓存计划、Test-MveRun、预检、独立审计四层强制核验 manifest.text_mode；正式缓存重跑至 `mve_r1_greedy/`，PAD 旧缓存保留为消融组（`wp7_run_mve.py --ablation-text-pad`，输出隔离为 *_ablation_pad，不构成 G1）。冻结前按文档/02 WP4 用 1 段音频做 greedy vs sampled AB 核验（`wp7_text_mode_ab.py`）。(d) **嵌套选择**：`grids.yaml` 新增 `inner_val_sessions: 32`——冻结 probe_train 顺序前 32 会话为 inner_val、其余 128 为 inner_train；C（逐种子）与最优层（种子均值 inner_val AUC argmax，并列取较小层号）均在内层选定后，以选定 C 在全量 probe_train 重训、于 probe_val 一次性评估；选择证据（逐层逐种子 inner_val AUC）写入 summary，独立审计验证声明的 argmax 一致性。登记：probe_val 已在被撤回的 PAD 变体上读取过一次，纠错重跑将再次读取（probe_val 非一次性确认集，causal_eval 25% 侧仍未触碰）。(e) **T4 标签风险**：`wp1_t4_audit.py` 固化 Backbiter 弱参考交叉核验为可复跑工具；PREREG #1 要求的生产链独立原始音频验证仍未完成，在其完成前 T4 结果附风险注记 | 用户撤回复盘：PAD 违反协议方向性利于 N1；Mimi 回退一帧后 T1 优势 −0.011750 → ≈−0.00763（未翻转但点估计与区间失效）；同一 40 会话身兼选 C、选层与最终报告；T4 启发式标签对 Backbiter ±0.16s F1≈0.503（高召回过标） | G1 执行协议；`configs/grids.yaml` mve 节（text_mode、inner_val_sessions）；`mve/alignment.py`（新）；`mve/dataset.py`；`mve/preflight.py`；`mve/run.py`；`wp7_run_mve.py`；`wp7_cache_mve.py`；Moshi runner；`wp7_audit_mve.py`；`wp7_text_mode_ab.py`（新）；`wp1_t4_audit.py`（新） | ① greedy 冒烟 1 run + greedy vs sampled AB（中层表征差异可忽略则冻结 greedy）；② 400 路 greedy 缓存重跑 + ingest；③ 正式 G1 重跑（预检强制 text_mode/time_alignment）+ 独立审计；④ PAD 消融复盘一次（量化伪影）；⑤ `wp1_t4_audit.py` 全量回传；⑥ **本机**重跑 `prereg_fingerprint.py` 回填 grids.yaml 新指纹（指纹以 Windows 工作区字节为准——容器侧因换行符差异不可回填；splits 各哈希必须保持不变）；判据阈值、划分、种子、层位、δ、基线集合、bootstrap 均不变 |
| 2026-07-18 | **已批准（用户裁决，依 LMGen 源码实测）** | **#8 R1 激活索引按 LMGen 在线制式锚定**：本机实测 `LMGen._step`（moshi/models/lm.py:693）在 offset p **先**把用户 token 写入 p+delay **再**读位置 p 送入 transformer；Moshiko 对方流 delays=[0,1,…,1]，故位置 p 已携带对方 q0 帧 p（q1–q7 为帧 p−1）；官方 run_inference 对首帧调用两次以覆盖 initial。R1 复放数组首位保留 initial，因此在线接收完对方帧 s 后的等价状态是 **acts[s+1]** 而非 acts[s]——#7 的行映射（acts 读 s、基线读 s−1）整体早一位。修正：**标签步 s ↔ acts[s+1]，Mimi/hazard/声学回到行 s**；acts[0]（纯 initial）永不使用；末标签步（无对应 acts 行）丢弃，可用步 0..n_steps−2；step 0 恢复合法。观测截止统一锚定为 (s+1)·τ（= 标签行 t 列的步末时刻），四路表征同足不变。**δ 登记**：T1 δ=240ms 仍为标签步索引整 3 步（y[s]=onset∈步 s+3），相对观测截止 (s+1)·τ 的**净前瞻为 [160,240) ms（2–3 时钟步）**；H1 "≥3 时钟步前瞻"的读法经用户 2026-07-18 裁决为**净毫秒读法**（步索引读法会随记账锚定漂移，非物理量）：G1 的 T1 判据点改用 **δ=400**（净 [320,400) ms），δ=240 降级为描述性附表（同协议全量计算、无分数包条目、剥离裁决字段、独立审计仅作形状与非裁决性核验）；`grids.yaml` t1_delta_ms 240→400、新增 t1_descriptive_deltas_ms:[240]。选择时点清白性：裁决时无人见过任何 δ400 探针数字 | 用户依 LMGen 源码裁定：LMGen 与 LMModel 的码本延迟机制一致，问题出在 R1 激活数组含开头 initial 却被按原始音频步直接索引；#7 的"观测 ≤ s·τ"锚定使探针比在线决策状态少看一帧对方语义流 | `mve/alignment.py`（锚定与行映射唯一权威）；`mve/dataset.py`；`mve/preflight.py` 行域口径；`wp7_run_mve.py` hazard/声学行映射与 protocol 声明；`wp7_audit_mve.py` 权威行集与 protocol 期望；哨兵测试全套 | 无需重跑缓存（数组本身不变，仅分析侧行映射）；greedy 缓存重跑与正式 G1 按 #7 清单执行即含本修正；δ 读法裁决后如改 δ=400 需在标签既有网格内换列（标签文件已含 δ400，无需重算） |
| 2026-07-18 | **已批准（用户裁决：方案 2+4）** | **#9 G0 层3 门槛冻结（四条件 Gate）**：确认集 = test 除**目录序前 20 探索会话**外的 **118 会话**，`wp1_g0_calibrate.py --confirmation` **一次性运行**（会话数 ≠118 或任一会话缺解码音频/处理失败即硬中止，不允许静默子集）。四条件：(1) 层1 协议全等 macro-F1 = 1.000 且逐类 F1 = 1.000；(2) 层2 双通道 VAD F1 均 ≥ 0.90（确认集重估）；(3) 层3 **语料级** macro-F1 ∈ **[0.48, 0.56]**（等价带 = val 点估计 0.5209 ± 0.04 ≈ ±3.4 倍语料级抽样 SE；**低出带 = fail，高出带 = red_flag_investigate**——暂停 Gate、登记调查探索侧渗漏/分布异常，由用户裁决处置，不自动通过）；(4) 层3 **会话级** macro-F1 的 **P10 ≥ 0.30**（val P10 0.344 − 余量；分位估计冻结为 numpy percentile method="linear"）。层3 职能重定位为"分布一致性检验 + VAD 误差非线性放大哨兵"，绝对质量由层1（正确性）+层2（VAD 域质量）承担；旧 0.85 门槛的同域假设对 Mimi 解码音频从未成立，正式退役 | 用户 2026-07-18 采纳方案 2（双侧等价带）+ 方案 4（尾部护栏）；依据 = val 295 会话分布（语料级 0.5209；会话级 mean 0.5089、SD 0.1298、P10 0.344、P50 0.504） | Gate G0 判据（00 §7）；`configs/events.yaml` g0.gate 块（f1_threshold 0.85 移除）；`events/g0_gate.py`（新，纯函数）；`wp1_g0_calibrate.py --confirmation` | 一次性运行 118 确认集 → `reports/g0_confirmation.json` + `g0_确认报告.md` 回传；本机重跑 `prereg_fingerprint.py`（events.yaml/grids.yaml 指纹更新，splits 哈希不变） |
| 2026-07-19 | **已批准（用户指令：等价工程加速）** | **#10 MVE greedy 缓存改用持久会话执行**：每张卡固定一个持久进程，Mimi 与 LM 各加载一次；200 个会话按会话级互斥分片，每个会话双声道各读取、编码一次，随后交换 self/other 顺序依次生成 agent0/agent1；WAV 一次传入显卡，Mimi 码与量化前潜表征先驻留显卡并整段回传；四层激活使用显卡双缓冲与 128 步分片，贪心 token 全程驻留显卡；LM 保持 `[1,K,1]` 单步输入并接入官方 `CUDAGraphed`，每个角色退出 streaming 上下文后显式确认状态已释放。该变更不改音频、时钟、延迟、文本反馈、层位、分片边界、输出 dtype、标签、划分、种子、基线、bootstrap 或 Gate 判据。切换前已完成且通过原四层护栏的 greedy 缓存继续有效；正式预检和独立审计登记并逐路核验历史与持久实现两个代码版本集合 | 旧实现每路重复加载 Mimi/LM、重复编码同一会话双声道，并在 7,500 个时钟步内执行高频 `.cpu()`/`.item()` 同步；双卡平均利用率约 65%–71%，中位每路 393 秒。用户要求只落实六项低风险工程优化，并在已有结果上续跑 400 路 | `runners/_shared/moshi_family.py`；`runners/moshi/run_batch.py`；`scripts/wp7_cache_mve.py`；`mve/preflight.py`；`wp7_run_mve.py`；`wp7_audit_mve.py`；文档/02 | 不重跑已有有效 greedy 缓存；仅做语法、定向单测和短冒烟，随后双卡从断点续跑至 400 路并按原顺序 ingest、正式 G1 与独立审计 |
| 2026-07-19 | **已批准（用户裁决 2026-07-19）** | **#11 G1 有效上下文截断重裁决**：(a) **取代全窗 greedy G1 的 N1 裁决**——7500 步运行超出 Moshiko 官方 context=3000 步（外部常数，官方配置 + 本机淘汰行为实测）。证据三重锚定：① 行 3000（初始 token 被环形缓存淘汰）出现 80/80 运行一致的巨范数（≈539）替代 sink 向量，与第 0 步初始状态方向余弦 0.9985、跨会话尖峰余弦 0.9999；② 行 6000（替代 sink 被淘汰）后深层范数持续抬升（L20 6.2→8.8）并进入跨会话公共吸引态（公共方向占比 0.50→0.85）；③ 冻结分数包分段复算：T4 优势 0–3000 步 +0.02949、3000–6000 步 +0.02197、6000+ 步 −0.06006 [−0.08027,−0.03765]（与 0–6000 段 CI 完全分离）；Mimi 潜表征全程稳定（范数 ≈1.55）构成天然对照，排除标签/音频侧解释。(b) **主判据窗冻结**：可用标签步 = 0..min(n_steps−2, **2998**)（acts 行 ≤2999，自动排除行 3000 尖峰及其后全部行）；train/eval 与四路表征（acts/Mimi/hazard/声学）同截；数值权威 `mve/alignment.py`（MODEL_CONTEXT_STEPS=3000、ANALYSIS_MAX_LABEL_STEP=2998），grids.yaml 冻结镜像 + 运行时一致性强制。**缓存不重跑**（流式 transformer 严格因果 ⇒ 前 3000 步与短跑逐位相同）。(c) **C 网格下扩** [1e-4,1e-3,0.01,0.1,1.0]——全窗运行 24/24（层×种子）格与 Mimi 基线全部选中旧下界 0.01，网格未包住最优点；嵌套选择规则不变。(d) **描述性附表扩充**（均非判据、无分数包条目、递归禁裁决字段）：`wp7_context_segments.py` 把分段诊断固化为确定性工具（对旧全窗分数包重建行→步映射，标签逐行全等 + 全窗段与旧 summary 零差异双硬校验）；matched-Mimi 信息下括号变体（`mimi_prev` 读行 s−1、最小合法步 1、同嵌套协议）——若探针明显高于 matched 而不高于官方 Mimi，则官方基线优势主要来自同帧连续潜表征输入特权。(e) **透明性登记**：分段诊断数字在本登记前已被读取（用户本机自冻结分数包复算）；截断点为外部常数且由三重机制锚定，非结果驱动的窗口挑选；新裁决来自截断窗上的完整重训与嵌套重选（层、C 均重选），不采用任何被窥探的分段数字。(f) 判据阈值（+0.02/+0.05）、划分、种子、层位、δ=400、三基线集合、bootstrap（1000/seed 0）均不变 | 用户 2026-07-19 裁定"全部低于 +0.02 因此 N1"不宜当作干净科学否证：最后两分钟约 23.5% 的 T4 样本把明确为正的备胎区结果拉入 N1，且激活取证与 context=3000 精确吻合 | G1 执行协议；`mve/alignment.py`（截断权威 + mimi_prev）；`configs/grids.yaml`（context_steps/analysis_max_label_step/probe_c_grid）；`mve/dataset.py`；`wp7_run_mve.py`（matched-Mimi + protocol.context_truncation）；`wp7_audit_mve.py`（行域/配置/协议/描述性核验）；`mve/run.py` 报告；`wp7_context_segments.py`（新）；哨兵测试 | ① 本机 `wp7_context_segments.py --bundle <旧全窗分数包>` 固化分段诊断（在重跑前执行，或用 git 提取旧 summary）；② 重跑 `wp7_run_mve.py`（截断窗 + 新 C 网格，无 GPU）+ `wp7_audit_mve.py`；③ PAD 消融同窗复跑一次；④ **本机** `prereg_fingerprint.py` 回填 grids.yaml 指纹（splits 哈希必须不变） |
| 2026-07-19 | **已批准（用户裁决：处置甲）** | **#12 G0 fail 处置甲（诊断 → 机制修复登记 → train 侧新确认）**：(a) G0 正式确认 **fail 维持登记，不作任何事后改判**（层2 语料级偏离 val 点估计 ≈3.7 倍设计 SE、逐类 F1 四类全降、层2 双通道平行下降——门在正常工作）；按文档/00 §7 冻结处置"修管线"执行。(b) **诊断集合限定** = val 295 + test 探索 20；118 确认集已一次性启封，**禁止**用于任何调参后重判。诊断工具 `wp1_g0_diagnose.py`：层2 FN 分解（漏检段类型/时长/响度、eot/bot/bc 金标边界帧的 FN±tol 波及比例）+ 修复候选响应面（Silero 阈值 × 解码域响度归一化）+ train 侧池清点；**全部输出为诊断非判据**。(c) **修复流程冻结**：诊断 → 具体修复参数另立变更记录登记（候选：解码域响度归一化后 VAD、解码域专用 Silero 阈值）→ 修复后管线在 val 重估分布并**重推等价带与门槛**（旧带以旧管线 val 分布为参照，管线变更后必须重推）→ 新确认集 = **官方 train 侧发布物可用且从未被 G0 读取的会话**（数量由 `--count-train-pool` 实测回填；`already_prepared` 必须为 0）→ 一次性运行裁决。层1 全等 1.000 要求不变。(d) 新确认完成前 G0 状态 = fail（已测保真度：层2 recall ≈0.828/0.841、层3 语料级 0.4769）；依赖 Mimi 域事件标注的下游设计（R2/R3 行为端点）须按该保真度携带敏感性预案 | 用户 2026-07-19 采纳处置甲；层2 分解显示精确率 0.985/0.979 而召回率 0.828/0.841——解码域漏检约 1/6 语音帧，边界类掉幅最大（eot 0.494→0.413、bot 0.446→0.381），"VAD 召回缺口 → 边界事件位移"因果链完整可修 | G0 处置流程；`wp1_g0_diagnose.py`（新）；未来 #13+ 的修复参数与新确认集登记 | ① 本机 `wp1_g0_diagnose.py`（val 全量分解）回传 `g0_diagnosis.json`/`g0_诊断报告.md`；② `--sweep`（默认 val 前 60 会话、阈值 {0.4,0.45,0.5} × {原始, −23 LUFS}）回传响应面；③ `--count-train-pool` 回传 train 池数量；④ 依结果登记修复参数变更记录后再谈新确认 |
| 2026-07-19 | **已批准（用户裁决）** | **#13 T4 生产标签人工原始音频核验抽样协议**（PREREG #1/#7 欠账的落实）：(a) **分层三格**：matched（生产 TURNEND ±0.5 s 内有 Backbiter 末点，一致格对照）/ prod_only（±1.0 s 内无任何 Backbiter 末点，生产多报嫌疑）/ backbiter_only（Backbiter 末点 ±1.0 s 内无任何生产事件，生产漏报嫌疑）；每格 40、每会话每格 ≤2、抽样种子 **20260719**；通道↔说话人映射取 ±0.5 s 贪心一对一匹配数较优者，非双说话人会话跳过并登记。(b) **盲听**：清单行序随机化、格标签密封于 key.json（听完前不得打开）；片段 = 原始音频事件时刻 ±4 s 立体声（左=ch0 右=ch1）；判定 = 目标侧说话人在标记时刻是否结束话轮（停止说话且 ~1.5 s 内未继续；对方接话或长静默均算结束）；verdict ∈ {y,n,u}，u 不入分母。(c) **汇总**：逐格确认率 + Clopper-Pearson 95% CI（`wp1_t4_human_audit.py ingest` → `reports/t4_human_audit.json`）。(d) **处置线（预先承诺）**：matched∪prod_only 合并确认率 **< 0.80**，或 backbiter_only 确认率 **≥ 0.50**（生产链漏掉的真实话轮末点过半）→ 触发 T4 标签生成链修订（另立变更记录，T4 相关裁决在修订完成前一律附风险注记）；两线均未触发 → PREREG #1/#7 的 T4 风险注记解除，Backbiter 分歧归因于弱参考自身噪声 | 用户 2026-07-19 批准；T4 为 G1 决定性目标而生产 vs Backbiter 在 ±1 s 容差下 micro-F1 仍只 0.4913——两个自动标注互相无法仲裁，只有人工原始音频可裁决谁对 | T4 标签可信度；`wp1_t4_human_audit.py`（新）；PREREG #1/#7 风险注记的解除条件 | ① 本机 `wp1_t4_human_audit.py sample`（≈120 片段）；② 人工盲听填 manifest.csv（约 1–2 小时）；③ `ingest` 回传 `t4_human_audit.json` + `t4_人工核验报告.md`；④ 依处置线执行后续 |
| 2026-07-19 | **已批准（用户裁决 D4/D5，处置甲第二步）** | **#14 G0 解码域 VAD 修复与 train 侧新确认**：(a) **修复参数**：G0 层2/3 的 Mimi 解码域 Silero 阈值 0.50 → **0.40**、无响度归一化（诊断响应面最优：60 会话上层2 F1 0.9332/0.9364、层3 0.5502）。**辖域限定**：仅解码音频（`events.yaml` g0.decoded_vad_threshold）；顶层原始音频事件本体继续用 vad.threshold 0.50；其他模型解码器不继承。(b) **门槛重推配方（冻结）**：修复后管线在 val 295 全量重估 → `--derive-gate` 按 band = [val语料级 − 0.04, +0.04]、p10_min = val P10 − 0.044（各取 4 位小数，与 #9 同余量构造）重推；旧 [0.48,0.56]/0.30 不直接平移；层1 =1.000 与层2 ≥0.90 不变。`--apply` 原地改写 events.yaml 并出 `reports/g0_gate_derivation.json`（绑定源 summary 哈希与阈值）。(c) **新确认集**：`wp1_g0_train_roster.py` 以种子 **20260717** 从 1986 个从未读取的官方 train 发布会话无放回抽 **300**（与 val 295 规模相当），写 `configs/splits/dualturn_train_confirm.json`——**生成即冻结**（脚本拒绝覆盖、纳入指纹、启封前不得读取内容）；`wp1_g0_prepare.py --roster` 导出（meta.split=train_confirm）→ decode_mimi → `wp1_g0_calibrate.py --confirmation-train` 一次性运行（roster/推导报告/配置三方一致 + 300 全齐 + 零失败硬校验）。(d) 新确认完成前 **G0 状态维持 fail**；改判须经 PREREG 结果登记 | 用户 2026-07-19 批准 D4（含五点澄清）与 D5；诊断证据 = 层2 召回 0.862 主损、FN 段比 TP 安静 ≈21 dB、边界波及 eot 58.7%/bot 64.2%/bc 77.2%、响应面 thr 0.4 最优 | `configs/events.yaml`（decoded_vad_threshold + train_confirm 块）；`wp1_g0_train_roster.py`（新）；`wp1_g0_prepare.py --roster`；`wp1_g0_calibrate.py`（解码域阈值 + --derive-gate/--apply + --confirmation-train）；`configs/splits/dualturn_train_confirm.json`（生成后） | ① roster 冻结 + 提交 + 指纹；② `--split val` 全量重估回传 g0_summary.json；③ `--derive-gate --apply` 回传推导报告并提交 events.yaml；④ prepare --roster + decode_mimi（GPU 数小时）；⑤ `--confirmation-train` 一次性运行回传；全程与 E1 并行（D6） |
| 2026-07-19 | **已批准（用户裁决 D1–D6）** | **#15 E1 全量设计冻结（五模型）**：(a) **上下文规格与缓存/分析窗**（`grids.yaml` e1.windows_s，#11 规程推广）：Moshi 240 s（context=3000 步）、PersonaPlex 240 s（论文规定同 Moshi）、dGSLM 122.88 s（论文严格规定）、MiniCPM-o 300 s（应力实测有效上下文 >1000 s，但密集三角测量显示 512 s 后疑似退化 → 保守取 300）、Freeze-Omni 300 s（有效 >1000 s，与 MiniCPM-o 对齐）；每模型窗 = min(600 s, 规格)。**公平性安排**：E1 判据均为"模型 vs 自身基线在同一行集"的组内比较，窗口差异为机制强制；跨模型并排另配 **公共窗 122.88 s 描述性切片**（common_window_s，全模型加算一份、非判据）以中和组成差异。(b) **会话集合（D2）**：E1 训练 = probe_train[0:400]、主评估 = probe_val[40:140]（全新 100）、MVE 历史集 = probe_val[0:40] 仅复现不并入主 CI、后备 108（probe_val[140:248]）使用须先登记；冻结前缀切片（candor.json 分池后按 UUIDv4 字典序）无新随机源；会话时长核查无需分层（全部 >240 s，均值/中位数各池一致）。权威实现 `e1/sets.py` + 哨兵测试。(c) **R2 生成制式（D3）**：温度 0.8、种子 0 冻结——锚点 = Moshi/PersonaPlex 官方音频温度 0.8（文本 0.7），兼顾 Freeze-Omni 官方运行命令 0.8；各模型官方配置调研全表登记于 文档/03 §D3。**全部模型的多温度、跨种子敏感性实验暂缓**，待主实验结论正面且稳定后再议。(d) **G2 操作化解释冻结**（`grids.yaml` e1.g2 + `e1/g2.py`）：top-3 层按各种子层 AUC 降序（并列取较小层号），三种子交集 ≥ 2；有效秩（rank-k 投影性能曲线）≤ 16；跨种子方向余弦 = 成对 |cos| 最小值 ≥ 0.8。(e) **时序（D6）**：G0 修复线与 E1 并行；DuplexConv 先冻结完整训练侧清单；**prereg-v1 tag 先于正式 E1 缓存启动**（前置：本条 + #14 roster + DuplexConv 清单 + 指纹回填） | 用户 2026-07-19 裁决 D1–D6 全批（D1 会话 300→500 且存储无虞；上下文规格来自论文规定与本机应力实测 4643fb4/565d420；双模型调用修复 2936f451） | `configs/grids.yaml` e1 块（新）；`src/floor_circuit/e1/sets.py`、`e1/g2.py`（新）；文档/03（决断点回填 + D3 调研表 + 公平性论证）；E1 缓存计划与探针网格引擎（后续轮实现） | ① DuplexConv gz/md 窥探回传 → 清单冻结脚本；② 五模型 E1 缓存计划器与全层 runner 扩展（与用户持久实现协同设计）；③ prereg-v1 材料齐后打 tag；④ Moshi E1 缓存启动 |

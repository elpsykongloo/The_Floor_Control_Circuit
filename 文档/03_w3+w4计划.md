# W3+W4 执行计划：E1 全量（五模型可解码性与几何）

状态：**v0.3（2026-07-21，PREREG #16 已登记：G0 退役、DuplexConv 顺延、E1 缓存 v2 规格冻结）**。
入口条件已满足：G1 于 2026-07-19 裁决 **full_e1**（T4 优势 +0.057173 [+0.044127,+0.069435]）。
科学定义以文档/00 §6-E1 与 §7-G2 为权威；本文落执行顺序、工程分解与已裁决参数。

**G0 已全线退役（PREREG #16，2026-07-21 用户裁决）**：G0 标记为初期探索阶段的不成熟想法
（判据建立在 Mimi 重建音频上，不具真值性质），一切修复/确认/质检/论文义务取消；本文余下的
G0 相关行仅作历史记录保留。配套登记事实：CANDOR 生态不存在人工话轮标注（AWS ASR 原始输出
+ Audiophile/Cliffhanger/Backbiter 三套算法化话轮模型；用户核验 34 分卷 / 1,656 会话 /
21,528 文件），Backbiter 定位修正为"算法化同侪参考"，CANDOR 标签效度支柱 = T4 人工核验
（#13）+ G1 优势佐证。

## 1. 目标与 Gate

- **E1（H1 全量）**：五模型 R1+R2 全层缓存 → 全层 × δ 网格 × T1–T5 线性探针（+2 层 MLP
  线性度对照）→ 基线族（声学 GRU / 编码器探针 / hazard / shuffled）→ 几何（类条件 PCA、
  秩扫描、事件对齐轨迹）→ 泛化矩阵 → 外部上界（VAP、DualTurn 报告值）→ Freeze-Omni
  浮现曲线。产出 Fig 2/3 + Table 2。
- **G2（冻结判据，00 §7；操作化解释冻结于 PREREG #15，实现 `e1/g2.py`）**：
  三种子 top-3 层交集 ≥ 2 ∧ 有效秩 ≤ 16 ∧ 跨种子方向余弦（成对 |cos| 最小值）≥ 0.8。
  过 → E2；不过 → 换位点（注意力/MLP 输出）重试一轮 → 仍不过 N1′。

## 2. 决断点裁决结果（2026-07-19，全部已批准并登记 PREREG #14/#15）

| # | 裁决 |
| --- | --- |
| D1 | 每模型缓存/分析窗 = min(600 s, context 规格)；会话总数 300 → **500**；存储无虞（D 盘 12T） |
| D2 | **E1 训练集 = probe_train[0:400]；主评估集 = probe_val[40:140]（全新 100）；MVE 历史集 = probe_val[0:40] 仅复现不并入主 CI；后备 108 使用须先登记**。冻结前缀切片（UUIDv4 字典序）无新随机源；时长核查无分层必要（各池均值 ≈35 min、全部 >240 s） |
| D3 | R2 制式 **温度 0.8、种子 0** 冻结（调研见 §5）；**全部模型的多温度、跨种子实验暂缓**，待主实验结论正面且稳定后再议 |
| D4 | G0 修复 = 解码域 Silero 阈值 **0.40**、无响度归一化，**仅 Mimi 解码音频**（原始音频事件本体保持 0.50，他模型解码器不继承）；val 295 重估后按冻结配方重推等价带与 P10；train 300 新确认模式已实现；启封前冻结 roster/种子/指纹；新确认前 G0 维持 fail |
| D5 | 新确认集 = 种子 **20260717** 从 1986 个从未读取的 train 会话抽 **300**（与 val 295 规模相当） |
| D6 | G0 修复线与 E1 **并行**；DuplexConv **先冻结完整训练侧清单**；**prereg-v1 先于正式 E1 缓存启动** |

## 3. 上下文规格登记表（已全部核实，#11 规程）

| 模型 | 窗口 | 依据 |
| --- | --- | --- |
| Moshi (moshiko) | **240 s**（3000 步） | 官方 config + 淘汰行为实测（PREREG #11） |
| PersonaPlex-7B-v1 | **240 s**（3000 步） | 论文规定同 Moshi；本机加载 checkpoint 实测 `lm.transformer.context=3000`，2 s greedy R1 清单复核一致（2026-07-20） |
| MiniCPM-o 4.5 | **300 s** | 应力实测有效上下文 >1000 s；密集三角测量显示 512 s 后疑似退化（提交 565d420）→ 保守取 300（用户裁决） |
| Freeze-Omni | **300 s** | 应力实测有效上下文 >1000 s（提交 4643fb4）→ 与 MiniCPM-o 对齐取 300 |
| dGSLM | **122.88 s** | 论文严格规定（四舍五入 123 s） |

### 窗口差异的公平性论证（登记于 PREREG #15）

1. E1 的一切判据量都是**组内比较**：模型 vs **自身**基线族在**同一行集**上（探针与基线
   同窗、同行、同协议）——窗口长短不改变组内对称性，任何模型都没有相对自己基线的
   信息优势。
2. 窗口差异是**机制强制**的（各模型 context 规格），不是任意选择；把短上下文模型硬拉到
   统一长窗才是真正的不公平（迫使其进入超上下文退化区——#11 的教训本身）。
3. 跨模型并排比较（Fig 2）的组成差异用**公共窗 122.88 s 描述性切片**中和：全部模型在
   [0, 122.88 s]（= 五模型窗口最小值）上加算一份组成匹配的探针/基线结果，非判据、
   进附表；主图仍展示各模型规格内全窗结果并标注窗口。
4. MiniCPM-o/Freeze-Omni 取 300 s（低于实测有效上限）只减少行数（统计精度），不引入
   有效性偏差；保守方向的裁剪不构成不公平。

## 4. 会话集合（PREREG #15 冻结，权威实现 `src/floor_circuit/e1/sets.py`）

- E1 训练：probe_train[0:400]；E1 主评估：probe_val[40:140]；MVE 历史：probe_val[0:40]
  （仅复现）；后备：probe_val[140:248]（108，使用须登记）；causal_eval 绝不读取。
- MVE 的 160+40 会话是 E1 集合子集，其 4 层 greedy 缓存直接复用（其余层补跑）。

## 5. R2 生成制式调研（D3 依据，2026-07-19 用户调研）

| 模型 | 官方运行配置 | 来源 |
| --- | --- | --- |
| Moshi | 音频温度 0.8，文本温度 0.7；音频 top-k 250，文本 top-k 25 | 官方 README（kyutai-labs/moshi） |
| PersonaPlex | 音频温度 0.8，文本温度 0.7；音频 top-k 250，文本 top-k 25 | 官方 offline.py（NVIDIA/personaplex） |
| MiniCPM-o 4.5 全双工 | 温度 0.7，top-p 0.8，top-k 100；TTS 另有温度 0.8 | 官方 modeling_minicpmo.py（HF openbmb/MiniCPM-o-4_5） |
| Freeze-Omni | README 运行命令 0.8、top-p 0.8、top-k 20；代码解析器默认 0.7、top-k 5 | 官方 README（VITA-MLLM/Freeze-Omni） |
| dGSLM | 单元温度 1.0、时长温度 1.0 | 官方 sample_speech_dlm.py（fairseq） |

结论：仓库的 0.8 最初随首轮脚手架（fa2a9a4）进入 PREREG，锚点 = 主受试 Moshi 的官方
音频温度，兼顾 Freeze-Omni 官方运行命令；各模型官方配置并不一致，跨模型统一温度本质是
折衷。**冻结 0.8 / 种子 0**；各模型敏感性（含 dGSLM 的 1.0 官方档）全部暂缓，待主结果
明晰后再议（用户 2026-07-19 裁决）。

## 6. 工作包与时间线

- **W3-P0（已完成，2026-07-20 闭合）**：双模型调用修复复跑（2936f451，见 §8 执行记录）；
  五模型上下文核实（4643fb4 + 565d420 + PersonaPlex checkpoint 实测）；T4 人工核验闭合
  （PREREG 结果登记）。
- **W3-P1（本轮交付）**：G0 修复线工具链（roster 冻结 / prepare --roster /
  --derive-gate / --confirmation-train）；E1 集合与 G2 判据库（`e1/sets.py`、`e1/g2.py`）；
  E1 设计冻结（grids.yaml e1 块）。
- **W3-P2（prereg-v1 前置，#16(b) 修订）**：前置 = #14 roster（已冻结）+ #15 + 本机指纹回填
  → **打 tag prereg-v1**。DuplexConv 下载等待条件已于 2026-07-23 解除：#31 固定完整
  发布物提交与 Windows 三类路径映射，全量审计覆盖 189 个音频 TAR 和 93,709 对
  WAV/JSON。下一步须先实现专用 TAR 成员索引并另行冻结训练清单；现有散装 JSON
  发现逻辑仍禁止直接使用。E1 主线与其零依赖。
- **WP-E1-1 缓存扩展（已实现并按 #17 加速，2026-07-22）**：计划器
  `wp_e1_cache_plan.py`（计划 v2：plan_id 内容寻址、git/代码/配置/权重摘要、逐会话音频
  sha256 + 240 s PCM 前缀指纹、双分片互斥并集校验、资源字节预算）；runner
  `moshi_family.py` v2 分支（全 32 层连续堆叠 [T,L,H] 分片 ×6/角色、512 步显存/锁页
  双缓冲整块回传、下一会话音频前缀预取、Mimi 单帧与 LM 单步 CUDA Graph 逐段释放、
  逐角色资源遥测、manifest v2、断点续跑校验、磁盘余量护栏、每进程独占一卡；按用户
  指令移除温控代码）；
  `wp_e1_cache_audit.py`（独立字面量审计）+ `wp_e1_cache_parity.py`（与历史 MVE 前 3000 步
  逐位核验）+ zarr 流式摄取。**全量统一重跑（#16(c)）**：不复用 MVE 4 层缓存，旧目录封存；
  输出 `e1_r1_greedy/`；随后 PersonaPlex 复用同一 v2 改造。
- **WP-E1-2/3 探针网格引擎（已实现，2026-07-22，PREREG #18–#24）**：`wp_e1_probe_grid.py`
  五阶段（labels→parity→acoustic→run→finalize）。协议裁决（#18）：C 网格沿 #11 五档；
  inner_val=probe_train[0:80]；种子=会话级 90% 子抽样⊕T1 负采样（G2 稳定性实质化）；
  10 规格（T1×6δ/T2/T3/T4/T5），多分类主指标 macro-OVR AUC，T5 stride=4；
  torch-GPU LBFGS 训练器与 sklearn 逐项等价 + 前置奇偶校验门；MLP 对照与打乱标签
  仅 ℓ*；有效秩 = rank-k 曲线 95% 保留；**G2 主目标预冻结 = T4**；基线 = Mimi 全规格 +
  hazard(T1/T2/T4) + 声学 GRU（二分类规格）。规模：960 探针单元 ≈ 4800 候选拟合 +
  960 正式重训。#19 依据本机 31.75 GiB 物理内存把执行链改为单进程共享层缓存：每层
  先载入训练侧 800 路并由双 GPU 按规格并行拟合，释放后载入评估侧 200 路；五档 C
  共用一次统计/显存搬运，同规格三种子共用一次评估装配，并保留拟合断点。格子 NPZ
  使用未压缩数组，保持键名与读取接口兼容，减少概率写盘的单线程压缩开销。正式命令：
  `uv run python scripts/wp_e1_probe_grid.py --stage run --num-shards 1 --devices cuda:0,cuda:1`。
  摄取走 `wp_e1_ingest.py`（计划驱动并行，wp5 堆叠回环兼容缺陷已修）。#20 修正生产
  标签后缀为 `<sid>.labels.parquet`；本机实查 500 个 E1 会话，339 个缺失、161 个历史产物
  经代码/配置指纹、标签摘要和源音频元数据核验后可复用。精确补算命令：
  `uv run python scripts/wp_e1_run_missing_events.py --session-list <data_root>\e1_probe\missing_label_sessions.txt`。
  #22 把声学输入改为只读取冻结的前 240 s（全量通道时长读取量减少 88.29%），增加
  NPY 原子断点与 `--acoustic-workers` 单命令工作池；标签增算支持互斥双分片。parity
  仅需冻结训练前 20 会话，可在其 40 路 Zarr 就绪后与剩余摄取重叠，并已在 GPU1 实跑通过。
  #23 修正 stacked 摄取的单块重复读改写：6 个源分片先顺序拼接一次，各层 Zarr 单块
  只写一次，最终布局与数值不变，理论块写入量由约 4.72 GB/路降至 0.79 GB/路；
  Windows 瞬时占用增加本路 `.partial` 精确清理与 2/4/8/16 s 退避重试，单路失败不再
  提前终止整个进程池。旧路径在 108/1000 触发 WinError 5 后已由新路径断点恢复。
  #24 将缺失标签的双通道完整波形改为逐通道载入与释放；首个 1879 s 会话压力探针
  用时 135.3 s，摄取同期保持约 316 路/h。正式前置调度据此采用摄取 3 worker 与标签
  4 个互斥分片重叠，标签算法、完整音频范围、指纹与事务输出均不变。
- **WP-E1-3 几何与 G2**：并入上条 finalize 阶段（秩扫描/方向余弦/evaluate_g2 正式裁决）；
  类条件 PCA 可视化与事件对齐轨迹（Fig 3）随报告轮补充。
- **WP-E1-4 泛化矩阵与外部上界**：跨会话/跨语料/跨语言迁移；VAP 推理与 DualTurn 汇表。
- **WP-E1-5 逐模型展开**：Moshi（W3）→ PersonaPlex（W3 末）→ MiniCPM-o / Freeze-Omni
  （W4，含浮现曲线）→ dGSLM（W4，agent 塔）。
- ~~并行 G0 线~~：**已退役（#16）**——train 300 确认已跑并 fail（09b4dee），跨分段剖析
  已固化（346ee02），此后不再投入；历史记录见 PREREG 结果登记。
- 里程碑：W3 末 = Moshi E1 全量 + G2 初判；W4 末 = 五模型 Fig 2 齐 + G2 正式裁决。

## 7. 预算与纪律

- GPU：Moshi E1 缓存（500 段 × 240 s × 双角色 × 32 层，持久会话实现）双卡并发复验
  GPU0/GPU1 中位 = **36.025/38.2645 步/秒**，243/257 会话的纯前向约 **11.24/11.19 h**；
  Mimi 单帧 CUDA Graph 在 240 s 上离散码与潜表征逐元素全等，编码 1.535×，预计每卡再省
  约 27 分钟。最终两会话集成冒烟为 353.2 s，按一次性模型载入摊薄、每会话双通道
  Mimi 图编码约 12.4 s 外推，正式端到端关键路径约 **12.1–12.3 h**，运行预留 12.5 h。
  MiniCPM-o/Freeze-Omni 按各自时钟另估；E1
  散热由用户人工保障。
- 剩余显存不启用双角色 batch=2：2 s 探针峰值分配 18.97 GB，速度仅为两个 batch=1 的
  0.516×，且从第 0 层起已出现逐元素差异；更大 Mimi 块也会改变离散码和连续潜表征。
- 存储（2026-07-21 修正双角色口径，#16）：Moshi 全层原始缓存 ≈ **0.79 TB**（单角色激活
  主体 750 MiB × 1000 路），加 Zarr 合计 ≈ **1.27 TiB**（按 MVE 实测压缩比外推）；
  512 步堆叠分片后总文件数约 0.9 万（原逐层布局 77.1 万）；D 盘 12T 无虞。
- 纪律不变：冻结参数改动先登记 PREREG；大文件不入 Git；`reports/` 回传反馈回路；
  causal_eval 在 E2 前绝不读取；每模型分析窗必须先过上下文规格表（§3）。

## 8. 执行记录

**2026-07-20 · W3-P0 MiniCPM-o / PersonaPlex 复跑闭合**：

- MiniCPM-o 无点 junction 已核验为指向原模型目录的 Windows Junction；纯读出模式修复上游
  `as_duplex()` 无条件初始化 TTS 的兼容性缺陷。6.016 s 音频完成 7/7 次预填，
  `current_time=1..7`，1 Hz 清单写出成功，TTS 未加载。
- PersonaPlex 官方 3.2 s 端到端复跑通过并生成有效音频；仓库 R1 runner 修复 Mimi
  缺少 `frame_size` 属性的兼容性缺陷。正式 greedy 冒烟完成 25 步、H=4096，
  激活与连续 Mimi 潜表征均为有限值，清单登记 context=3000。
- 定向测试 22/22、全量测试 246/246、全仓 Ruff 检查全部通过。详细证据见
  `reports/w3_p0_双模型复跑验证.md` 与同名 JSON；大型产物保存在
  `D:\data_storage\The_Floor_Control_Circuit\validation`。

**2026-07-21 · G0 线收口与退役**：

- train 300 一次性确认 fail（层2 过、层3 语料级 0.4846 出带 / P10 0.3233 低线，09b4dee）；
  跨分段剖析固化（val 块组成偏易、等价带可交换性前提双重证伪，346ee02）。
- 用户裁决 G0 全线退役（PREREG #16(a)）：初期探索的不成熟想法，基于 Mimi 重建音频、
  不具真值性质；一切后续义务取消。CANDOR 溯源核验（无人工话轮标注）同轮登记。

**2026-07-21 · WP-E1-1 缓存 v2 交付（云端实现，待本机冒烟）**：

- 交付件见 §6 WP-E1-1 条目；六级冒烟阶梯与正式双卡长跑流程见 PREREG #16 重跑列。
- 上下文规格、会话集合、R2 制式、G2 判据均沿用 #15 冻结值不变。

**2026-07-22 · WP-E1-1 正式缓存闭合 + WP-E1-2/3 交付**：

- 用户回传：E1 正式双卡缓存审计 passed（1000/1000 路、789.53 GB、两卡 486/514 路、
  中位 36.32/38.37 步/秒、峰值显存 ≈17.65 GB；95d57f2）；计划/冒烟/逐位一致性报告
  已在 3ade51e 推送。
- 云端交付探针网格引擎全套（见 §6 WP-E1-2/3 条目与 PREREG #18–#24）；
  资源调度：ingest 期间已完成 1 个 acoustic worker 的 1000 路产物，并提前通过前 20 会话
  parity；单会话标签压力探针确认资源影响可控后，已把缺失标签扩为 4 个互斥分片与
  ingest 重叠 → labels 复查 → run（单进程双 GPU）→ finalize。

### 2026-07-23（第二十轮，云端）：E1 正式收口后的探索性转向（PREREG #33/#34；本机并行支线见 #31 DuplexConv 闭合与 #32 几何解剖）

- 本机回传：G2 正式 **fail**（三条件两过一挂：top-3 交集 3 层、方向最小 |cos| 0.9757
  均通过；保守有效秩 128 ≫ 16 失败）。#30 事后诊断把过线位置精确到 57–84，
  并确认前 16 个主成分只解释 38–45% 激活方差——失败集中在"与主方差基对齐"。
- 用户裁决：该形态不是预注册的降级形态，开启独立探索轨道，不必完全遵守预注册协议；
  正式 G2=fail 原样保留，探索与正式分轨登记（论文中分节呈现）。
- 云端交付 **WP-E1X（PREREG #33）**：`wp_e1x_suite.py` 七阶段
  （geometry/leadtime/decompose/trajectory/t2h/anatomy/finalize），
  共享库 `src/floor_circuit/e1x/`；核心问题 = ① T4 信息比声学早多少可读（先知曲线）、
  ② 决策变量动力学与内部证据领先量、③ +0.0659 优势的声学共享/内部独有分解与
  残差有效秩（若独有部分低秩 → G2 字面失败源于声学方差污染基底）、
  ④ 方向几何（T1×δ 旋转、跨目标转移、PC 质量谱=方差-信息解离图）、
  ⑤ T2 视野边界与 T4 错误解剖（为 E4 刺激设计供靶）。
- 云端交付 **WP-E2L（PREREG #34）**：E2-lite 方向级因果试点先于换位点重试——
  `wp_e2_lite_plan.py`（260 运行计划）→ `runners/moshi/run_steer.py`
  （R2 生成 + L29 恒等相加注入 α·s_v·v̂，LMGen 官方采样，manifest 断点）→
  `wp_e2_lite_analyze.py`（VAD 行为指标、配对剂量-反应、随机方向参照）。
  α=0 基线顺带缓存 L28–31 逐步激活 = R2 观察性探针数据。
- 判读树（预登记）：E2-lite 有单调剂量-反应 → 全量 E2（G3 主线复活，方向粒度）；
  无效应 → 换位点/头级 EAP 分解，紧凑性改在模型组件基上衡量。

### 2026-07-24（第二十二轮，云端）：E2-lite 深挖 + E2 确认臂 + 先知扩展 + R2 探针（PREREG #40）

- 背景：E1-X 与 E2-lite 全量闭合（#39）——T4@L29 方向 rank-1 因果充分、剂量单调
  （发声占比 −0.121→+0.300，ρ=0.871）、方向特异（随机 ≤0.03；diffmeans 双向皆降）。
  用户指令：完整实现下一轮全部代码；缓存归档议题取消（存储充足）。
- 云端交付四件（全部探索性，基线复用 E2-lite baseline，不触 causal_eval）：
  (a) `wp_e2_lite_deep.py`：语境三分 + SDT（d′/criterion c，判读预登记：c 单调而
  d′ 保持 → 判据旋钮）+ 文本质量（PAD/熵/段长）+ 与同 20 会话人类双方分布的 JSD；
  (b) `wp_e2_confirm_plan.py` → `runners/moshi/run_steer_confirm.py`（eager 参考语义）
  → `wp_e2_confirm_analyze.py`：必要性（投影钳制 μ_v）、时间特异性（respond /
  user_speech 门控注入）、轴特异性（T1_d800 / T5:SPEAK）、层特异性（L20/L31 重标定注入），
  13 条件 × 20 会话 = 260 运行；
  (c) 先知曲线 shift 网格扩至 4.96 s（{31,37,50,62} 步），断点新增 min_anchor_step
  守卫，网格变更后旧档自动重算；
  (d) `wp_e2_lite_r2_probe.py`：baseline 缓存激活上的 R1→R2 零样本读出与
  自主接话行为预测（决策变量-行为耦合）。
- 运行顺序与回传清单见本轮会话答复；G3 正式裁决材料 = (b) 四组结果 + E2-lite 主结果。

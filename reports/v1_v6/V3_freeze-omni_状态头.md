# V3 · Freeze-Omni 状态头冒烟

结论：**通过**（2026-07-17，用户本机实测）。

## 实测证据

- 13 个 160 毫秒音频块均采到状态头 logits，全部为有限值。
- 由 logits 重算的概率与模型内部概率最大误差 1.19×10⁻⁷。
- 状态头参数形状 **[4, 3584]**，官方判决使用**前三维** logits。
- 1.60–1.76 秒处采得 logits [1.046875, 3.656250, −5.375000]，对应 state-1 概率 0.931359，模型同步从继续听（cl）转为开始说（ss）。

## 判据核对（文档/02 §WP3-V3）

| 判据 | 结果 |
| --- | --- |
| chunk 长度确认 | ✅ 160 ms（已回填 configs/grids.yaml） |
| 状态头 logits 可导出且数值一致 | ✅（重算误差 1.19e-7） |
| 三态语义确认 | ✅ 判决用前三维；观测到 cl→ss 转移（state 0 = 继续接收的官方语义与观测相容，E1 前在 readout 里再交叉核对一次枚举命名） |

## 后续动作

1. 把冒烟脚本的采集循环接入 `runners/freeze_omni/run.py` 的 `collect_stream()`（或回传脚本由远端接线）。
2. E2 的"注入位点 patching"需要骨干各层 chunk 末帧 hidden state——在接线时顺带确认 hook 位点可注册。

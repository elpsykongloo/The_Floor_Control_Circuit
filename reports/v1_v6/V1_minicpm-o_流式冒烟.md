# V1 · MiniCPM-o 4.5 纯音频流式冒烟

结论：**通过**（2026-07-17，用户本机实测）。

## 实测证据

- 2.022 秒音频按 1 秒切为 3 块；预填全部成功。
- 采到 3 条 `is_listen` 决策，`current_time` 为 1、2、3，决策序列为"听、说、说"。
- 每个时钟步只传入 `audio_waveform`，没有视频帧或文本块。
- 这里的 1 Hz 指模型的一秒输入时钟；离线冒烟没有按墙钟等待。

## 判据核对（文档/02 §WP3-V1）

| 判据 | 结果 |
| --- | --- |
| 官方本地链路可跑纯音频流式 | ✅ |
| 1 Hz 决策序列可导出且与音频时长对齐 | ✅（3 块 → 3 条决策） |
| 骨干各层 hook 可注册 | ⏳ 未在本次冒烟覆盖，E1 缓存前补验 |

## 后续动作

1. 把冒烟脚本的采集循环接入 `runners/minicpm_o/run.py` 的 `collect_stream()`（或回传脚本由远端接线）。
2. E1 前补一次骨干 hook 注册 sanity（层数 × hidden 维度 × 与 1 Hz 决策的对齐）。
